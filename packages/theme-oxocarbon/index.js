/**
 * Host half of the Oxocarbon theme layer. The whole feature is a browser-side
 * token override, so the host half only needs to exist for the loader row.
 */
export const name = 'theme-oxocarbon'

/** No host-side state, service, or configuration. */
export function apply() {}
